package com.utp.cablescontrol;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

public class Home extends AppCompatActivity {

    //private lateinit var mBinding:ActivityMainBinding
    //private lateinit var mAdapter:ZonaAdapter
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_home);

        //setupRecyclerView()
    }



    //private fun setupRecyclerView()
    //{
    //lista vacía
    //mAdapter=ComicAdapter(mutableListOf(),this)

    //nro. de columnas 2
    //mGridLayout= GridLayoutManager(this,1)

    //llamar listado de zonas
    //getComics()

    //se pasar a configurar el RecyclerView
    //mBinding.recyclerView.apply {

    //el tamaño no sea modificable
    //setHasFixedSize(true)

    //asignando valores
    //adapter=mAdapter
    //layoutManager=mGridLayout

    //}
}
